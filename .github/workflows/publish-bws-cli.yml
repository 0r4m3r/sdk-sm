name: Publish bws-cli

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  publish:
    name: Publish bws-cli (linux)
    runs-on: ubuntu-24.04
    steps:
      - name: Confirm published release
        run: |
          echo "Release published: ${{ github.event.release.tag_name }}"
          echo "Assets:"
          printf '%s\n' "${{ toJson(github.event.release.assets) }}"
